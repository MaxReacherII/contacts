import React from 'react';
import Typography from "@mui/material/Typography";

export default function Modal() {
    return(
        <Typography>Confirm, delete, add. Reusable modal</Typography>
    )
}